export function ContactInfo(props){
        return(
                <>
                <h1>Name:{props.name} Contact Information</h1>
                <p>{props.des} </p>
                <button>Contact</button>
                </>
        )
}
